apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio
  namespace: koku
  labels:
    app: minio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
    spec:
      containers:
        - name: minio
          image: minio/minio:RELEASE.2025-01-20T14-49-07Z
          ports:
            - containerPort: 9000
            - containerPort: 9090
          envFrom:
            - configMapRef:
                name: koku-config
          command:
            - minio
            - server
            - /home/kminio/data
            - --console-address
            - ":9090"
          volumeMounts:
            - name: minio-storage
              mountPath: /home/kminio/data
      volumes:
        - name: minio-storage
          persistentVolumeClaim:
            claimName: koku-pvc  
