apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: koku-pvc
  namespace: koku
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi 
  storageClassName: gp3  
